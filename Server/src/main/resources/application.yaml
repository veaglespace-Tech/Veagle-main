spring:
  application:
    name: VeagleSpaceTech

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/veagleDB?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:1234}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: singareakshay2004@gmail.com
    password: rcckpmkrnkopufmv
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com

jwt:
  secret: mySuperSecretKeyForJwt1234567890123456

admin.secret: VeagleSpace
