spring:
  application:
    name: VeagleSpaceTech

  datasource:
    # HOSTINGER VPS: Update these with your Hostinger MySQL credentials
    # Local dev: jdbc:mysql://localhost:3306/veagledb
    url: ${DB_URL:jdbc:mysql://localhost:3306/veagledb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Veagle@123}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

veagle:
  mail:
    otp:
      - email: ${MAIL_USER:noreply1@veaglespace.com}
        password: ${MAIL_PASS:Veagle@123}
      - email: ${MAIL_USER_2:noreply2@veaglespace.com}
        password: ${MAIL_PASS_2:Veagle@123}

    careers:
      - email: ${MAIL_CAREERS:careers@veaglespace.com}
        password: ${MAIL_CAREERS_PASS:Veagle@123}

    support:
      - email: ${MAIL_SUPPORT:support@veaglespace.com}
        password: ${MAIL_SUPPORT_PASS:Veagle@123}


  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB

jwt:
  secret: ${JWT_SECRET:VeagleJwtSecretKey2026UltraSecure123!}

admin:
  secret: ${ADMIN_SECRET:admin123}

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dbehhnhhi}
  api-key: ${CLOUDINARY_API_KEY:266323574446942}
  api-secret: ${CLOUDINARY_API_SECRET:jiEAtmhR144qAZFvZLIWDwsRrKA}

server:
  port: 8080

